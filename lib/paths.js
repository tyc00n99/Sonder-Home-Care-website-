// Prefix for static assets referenced by URL (video, poster) so the site
// works both at a domain root and under a GitHub Pages project path.
export const bp = process.env.NEXT_PUBLIC_BASE_PATH || '';
